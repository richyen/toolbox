version: '3'

services:
  assessment:
    image: assessment
    build: ../
    container_name: l1
    privileged: true
    stdin_open: true
    tty: true
    environment:
      - "USEDOCKER=1"
    volumes:
      - ${PWD}:/docker
    entrypoint:
      - "/docker/assessment_setup.sh"
